# Local dev: proxy /api to your Flask backend (default http://localhost:5000)
# Or point at Railway while Flask is still deployed there:
# VITE_API_PROXY_TARGET=https://budget-tracker-production-5caa.up.railway.app
VITE_API_PROXY_TARGET=http://localhost:5000
