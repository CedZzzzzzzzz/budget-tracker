{
  "version": "0.5.0",
  "scripts": {
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "python -m pytest",
    "test:frontend": "npm --prefix frontend run lint && npm --prefix frontend run build && npm --prefix frontend test"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.2",
    "postcss": "^8.5.16",
    "tailwindcss": "^4.3.2"
  }
}
